---
name: <skill-name>
description: <what this skill does and when to use it>
---

# <skill-name>

## Workflow

1. Read only the context needed for the request.
2. Select script/reference files relevant to this task.
3. Execute deterministic steps first.
4. Validate output before returning.

## Resource Routing

- Use `scripts/` for deterministic repeated actions.
- Use `references/` for large domain-specific details.
- Use `assets/` for reusable output resources.

## Output Contract

- Return concrete deliverables and verification status.
- Call out residual risks and missing checks.

